# CLOUD-320: API Contracts & Service Interfaces

> **Generated by**: OWL Technical Writer  
> **Date**: 2026-06-07

---

## 1. Backend API (Spring Boot WebFlux) — Port 8080

### 1.1 Authentication Endpoints

```
POST /api/auth/login
Request:  { "email": "string", "password": "string" }
Response: { "token": "string", "user": UserDto, "tenant": TenantDto }

POST /api/auth/register
Request:  { "email": "string", "password": "string", "name": "string", "companyName": "string" }
Response: { "token": "string", "user": UserDto }

POST /api/auth/forgot-password
Request:  { "email": "string" }
Response: { "message": "string" }

POST /api/auth/reset-password
Request:  { "token": "string", "newPassword": "string" }
Response: { "message": "string" }

GET  /api/auth/oauth/google
Query:   code, state
Response: Redirect with JWT token

GET  /api/auth/oauth/facebook
Query:   code, state
Response: Redirect with JWT token
```

### 1.2 Tenant & Company Management

```
GET    /api/tenants/current
Headers: Authorization: Bearer {JWT}
Response: TenantDto

PUT    /api/tenants/{id}
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Request:  TenantDto
Response: TenantDto

GET    /api/companies
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Response: Page<CompanyDto>

POST   /api/companies
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Request:  CompanyDto
Response: CompanyDto

GET    /api/companies/{id}
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Response: CompanyDto

PUT    /api/companies/{id}
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Request:  CompanyDto
Response: CompanyDto
```

### 1.3 Contacts

```
GET    /api/contacts
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Query:   page, size, search, tag
Response: Page<ContactResponseDto>

POST   /api/contacts
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  ContactRequestDto
Response: ContactResponseDto

GET    /api/contacts/{id}
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: ContactResponseDto

PUT    /api/contacts/{id}
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  ContactRequestDto
Response: ContactResponseDto

DELETE /api/contacts/{id}
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: 204 No Content
```

### 1.4 Products

```
GET    /api/products
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Query:   page, size, category, search
Response: Page<ProductResponseDto>

POST   /api/products
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  ProductRequestDto
Response: ProductResponseDto

GET    /api/products/{id}
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: ProductResponseDto

PUT    /api/products/{id}
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  ProductRequestDto
Response: ProductResponseDto

DELETE /api/products/{id}
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: 204 No Content
```

### 1.5 Orders

```
GET    /api/orders
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Query:   page, size, status, contactId
Response: Page<OrderResponseDto>

POST   /api/orders
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  OrderRequestDto
Response: OrderResponseDto

GET    /api/orders/{id}
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: OrderResponseDto

PUT    /api/orders/{id}/status
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  { "status": "string" }
Response: OrderResponseDto
```

### 1.6 Invoices

```
GET    /api/invoices
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Query:   page, size, status, dateFrom, dateTo
Response: Page<InvoiceResponseDto>

POST   /api/invoices
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  InvoiceRequestDto
Response: InvoiceResponseDto

GET    /api/invoices/{id}
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: InvoiceResponseDto
```

### 1.7 Pipelines (CRM Kanban)

```
GET    /api/pipelines
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: Page<PipelineDto>

POST   /api/pipelines
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  PipelineCreateRequest
Response: PipelineDto

GET    /api/pipelines/{id}/kanban
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: { "stages": [ { "id": "string", "name": "string", "cards": [PipelineKanbanCardDto] } ] }

POST   /api/pipelines/{id}/stages
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  PipelineStageCreateRequest
Response: PipelineStageDto

PUT    /api/conversations/{id}/stage
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  MoveConversationRequest
Response: ConversationPipelineStateDto
```

### 1.8 Chat / Conversations

```
GET    /api/conversations
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Query:   page, size, status, assignedTo, channel
Response: Page<ConversationDto>

GET    /api/conversations/{id}/messages
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: Page<MessageDto>

POST   /api/conversations/{id}/messages
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  MessageCreateRequest
Response: MessageDto
```

### 1.9 Evolution API Webhook

```
POST /api/evolution/webhook
Headers: X-Api-Key: {EVOLUTION_API_KEY}
Body:    EvolutionWebhookEvent
Response: 200 OK

POST /api/evolution/instances
Headers: Authorization: Bearer {JWT}
Request:  { "instanceName": "string", "channel": "whatsapp" }
Response: { "instanceId": "string", "qrCode": "string" }

GET  /api/evolution/instances/{id}/status
Headers: Authorization: Bearer {JWT}
Response: { "status": "connected" | "disconnected" | "connecting" }
```

### 1.10 Internal Billing API

```
POST /api/internal/billing/process
Headers: X-Internal-Key: {INTERNAL_API_KEY}
Request:  { "tenantId": "number", "subscriptionId": "number", "amount": "number" }
Response: { "transactionId": "string", "status": "success" | "failed" }

POST /api/internal/billing/webhook/wompi
Headers: X-Signature: {HMAC_SIGNATURE}
Body:    WompiWebhookEvent
Response: 200 OK
```

### 1.11 Dashboard Stats

```
GET /api/dashboard/stats
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: DashboardStatsDTO {
    salesStats: { total: number, trend: number, chartData: DataPoint[] }
    customersStats: { total: number, new: number, trend: number }
    chatbotStats: { conversations: number, resolved: number, avgResponseTime: number }
    inventoryStats: { totalProducts: number, lowStock: number }
    todayAppointments: TodayAppointmentDTO[]
    recentOrders: RecentOrderDTO[]
    unreadChats: UnreadChatSummaryDto[]
}
```

### 1.12 Subscriptions

```
GET    /api/subscriptions
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Response: Page<SubscriptionResponse>

POST   /api/subscriptions
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Request:  SubscriptionCreateRequest
Response: SubscriptionResponse

PUT    /api/subscriptions/{id}/limits
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Request:  SubscriptionLimitsUpdateRequest
Response: SubscriptionResponse

PUT    /api/subscriptions/{id}/modules
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Request:  SubscriptionModulesUpdateRequest
Response: SubscriptionResponse
```

### 1.13 RBAC

```
GET    /api/rbac/roles
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Response: Page<RoleDto>

POST   /api/rbac/roles
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Request:  RoleRequestDto
Response: RoleDto

GET    /api/rbac/permissions
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Response: Page<ActionPermissionDTO>

PUT    /api/rbac/roles/{id}/permissions
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Request:  PermissionGrantDTO[]
Response: RoleDto

GET    /api/rbac/users/{id}/permissions
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}
Response: UserPermissionsDto
```

### 1.14 Workflows (Automation)

```
GET    /api/workflows
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: Page<WorkflowDto>

POST   /api/workflows
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  WorkflowRequest
Response: WorkflowDto

POST   /api/workflows/{id}/execute
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: { "executionId": "string", "status": "running" }

GET    /api/workflows/{id}/executions
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: Page<WorkflowExecutionLogEntity>
```

### 1.15 Marketing Campaigns

```
GET    /api/campaigns
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Query:   page, size, status, channel
Response: Page<CampaignDto>

POST   /api/campaigns
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  CampaignDto
Response: CampaignDto

POST   /api/campaigns/{id}/send
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: { "campaignId": "string", "status": "sending", "totalRecipients": "number" }

GET    /api/campaigns/{id}/history
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: Page<CampaignSendLogEntity>
```

### 1.16 Sending Lists

```
GET    /api/sending-lists
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: Page<SendingListEntity>

POST   /api/sending-lists
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  SendingListEntity
Response: SendingListEntity

POST   /api/sending-lists/{id}/contacts
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  { "contactIds": ["number"] }
Response: { "added": "number" }
```

### 1.17 Tags

```
GET    /api/tags
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: Page<TagEntity>

POST   /api/tags
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  TagRequest
Response: TagEntity

POST   /api/contacts/{id}/tags
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  { "tagIds": ["number"] }
Response: ContactResponseDto
```

### 1.18 Channels

```
GET    /api/channels
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: Page<ChannelDto>

POST   /api/channels
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  ChannelCreateRequest
Response: ChannelDto

GET    /api/channels/{id}/config
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: ChannelConfigDto

PUT    /api/channels/{id}/config
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  ChannelConfigDto
Response: ChannelConfigDto
```

### 1.19 Calendar & Appointments

```
GET    /api/appointments
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Query:   dateFrom, dateTo, userId, status
Response: Page<AppointmentEntity>

POST   /api/appointments
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  AppointmentEntity
Response: AppointmentEntity

GET    /api/calendar/events
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Query:   start, end
Response: CalendarEvent[]

POST   /api/calendar/events
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  CalendarEventDto
Response: CalendarEventDto
```

### 1.20 HR & Payroll

```
GET    /api/hr/employees
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: Page<EmployeeDto>

POST   /api/hr/employees
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Request:  EmployeeCreateDTO
Response: EmployeeDto

GET    /api/payroll/periods
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: Page<PayrollPeriodDto>

POST   /api/payroll/periods/{id}/process
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: { "processed": "number", "errors": "number" }

GET    /api/payroll/receipts/{id}
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: PayrollReceiptDto

GET    /api/payroll/receipts/{id}/pdf
Headers: Authorization: Bearer {JWT}, X-Tenant-Id: {tenantId}, X-Company-Id: {companyId}
Response: application/pdf
```

---

## 2. Billing Service (Go) — Port 8086

### 2.1 Billing Endpoints

```
POST /billing/process
Internal Call from backend API
Request:  { "tenantId": "number", "subscriptionId": "number", "amount": "number", "currency": "string" }
Response: { "transactionId": "string", "status": "success" | "failed", "wompiReference": "string" }

GET  /billing/subscription/{id}
Internal Call
Response: { "id": "number", "status": "active" | "suspended" | "cancelled", "nextBillingDate": "date" }

POST /billing/webhook/wompi
Public (Wompi callback)
Headers: X-Signature: {HMAC}
Body:    WompiWebhookEvent
Response: 200 OK

GET  /billing/invoices
Internal Call
Query:   tenantId, status, dateFrom, dateTo
Response: Invoice[]

POST /billing/invoices
Internal Call
Request:  { "tenantId": "number", "items": [...], "total": "number" }
Response: Invoice
```

### 2.2 Wompi Integration

```
POST https://sandbox.wompi.co/v1/transactions
Headers: Authorization: Bearer {WOMPI_PRIVATE_KEY}
Request:  {
    "amount_in_cents": "number",
    "currency": "COP",
    "customer_email": "string",
    "reference": "string",
    "payment_method": { "type": "PSE|NEQUI|CARD", ... }
}
Response: { "data": { "id": "string", "status": "APPROVED|DECLINED|PENDING" } }
```

---

## 3. Scheduler Service (Java/Spring) — Port 8085

### 3.1 Appointment Endpoints

```
GET    /scheduler/appointments
Headers: Authorization: Bearer {JWT}
Query:   tenantId, companyId, userId, dateFrom, dateTo
Response: AppointmentEntity[]

POST   /scheduler/appointments
Headers: Authorization: Bearer {JWT}
Request:  AppointmentEntity
Response: AppointmentEntity

PUT    /scheduler/appointments/{id}
Headers: Authorization: Bearer {JWT}
Request:  AppointmentEntity
Response: AppointmentEntity

DELETE /scheduler/appointments/{id}
Headers: Authorization: Bearer {JWT}
Response: 204 No Content
```

### 3.2 Availability Endpoints

```
GET    /scheduler/availability/{userId}
Headers: Authorization: Bearer {JWT}
Query:   dateFrom, dateTo, serviceId
Response: AvailabilitySlot[]

POST   /scheduler/availability
Headers: Authorization: Bearer {JWT}
Request:  AvailabilityTemplateEntity
Response: AvailabilityTemplateEntity
```

### 3.3 Billing Scheduler Endpoints

```
POST /scheduler/billing/trigger
Internal Call
Request:  { "tenantId": "number", "subscriptionId": "number" }
Response: { "status": "triggered" }

GET  /scheduler/billing/jobs
Internal Call
Response: ScheduledJobEntity[]
```

---

## 4. Notification Service (Java/Spring) — Internal

### 4.1 Kafka Consumer Topics

```
Consumer Group: notification-service
Topics:
  - notification.events
  - billing.events
  - payroll.events
  - campaign.events
```

### 4.2 Email Templates

```
Template: notification.ftl
Variables: { "userName", "message", "actionUrl", "companyName" }

Template: payroll-receipt.ftl
Variables: { "employeeName", "period", "devengos", "deducciones", "total" }

Template: recover-password.ftl
Variables: { "userName", "resetLink", "expiryHours" }

Template: register.ftl
Variables: { "userName", "verificationLink", "companyName" }

Template: whatsapp-disconnection.ftl
Variables: { "userName", "instanceName", "reconnectLink" }

Template: whatsapp-handoff.ftl
Variables: { "customerName", "agentName", "message" }
```

### 4.3 Internal API

```
POST /notifications/send
Internal Call
Request:  {
    "type": "email" | "whatsapp" | "push",
    "recipient": "string",
    "template": "string",
    "variables": { "key": "value" },
    "tenantId": "number"
}
Response: { "notificationId": "string", "status": "sent" | "queued" }
```

---

## 5. Chat Socket Service (Node.js/Socket.IO) — Port 3001

### 5.1 WebSocket Events

```
Client -> Server:
  - "join" { room: string, token: string }
  - "leave" { room: string }
  - "message" { room: string, content: string, type: "text" | "image" | "file" }
  - "typing" { room: string, isTyping: boolean }
  - "read" { room: string, messageId: string }

Server -> Client:
  - "message" { id: string, room: string, sender: string, content: string, timestamp: string }
  - "typing" { room: string, user: string, isTyping: boolean }
  - "presence" { user: string, status: "online" | "offline" }
  - "notification" { type: string, data: object }
```

### 5.2 REST Endpoints

```
GET    /api/chat/rooms
Headers: Authorization: Bearer {JWT}
Response: ChatRoom[]

GET    /api/chat/rooms/{id}/messages
Headers: Authorization: Bearer {JWT}
Query:   page, size
Response: Page<Message>

POST   /api/chat/rooms/{id}/messages
Headers: Authorization: Bearer {JWT}
Request:  { "content": "string", "type": "text" }
Response: Message

POST   /api/chat/notify
Internal Call
Request:  { "room": "string", "event": "string", "data": object }
Response: 200 OK
```

### 5.3 Kafka Consumer Topics

```
Consumer Group: chat-socket-service
Topics:
  - chat.events
  - notification.events
```

---

## 6. Evolution API (Node.js) — Port 8082

### 6.1 Instance Management

```
POST /instance/create
Headers: Authorization: Bearer {EVOLUTION_API_KEY}
Request:  { "instanceName": "string", "channel": "whatsapp" }
Response: { "instanceId": "string", "qrCode": "string" }

GET  /instance/connectionState/{instanceName}
Headers: Authorization: Bearer {EVOLUTION_API_KEY}
Response: { "state": "open" | "close" | "connecting" }

DELETE /instance/delete/{instanceName}
Headers: Authorization: Bearer {EVOLUTION_API_KEY}
Response: 200 OK
```

### 6.2 Message Sending

```
POST /message/sendText/{instanceName}
Headers: Authorization: Bearer {EVOLUTION_API_KEY}
Request:  {
    "number": "string",
    "text": "string",
    "options": { "delay": "number", "presence": "composing" }
}
Response: { "key": { "remoteJid": "string", "id": "string" }, "messageTimestamp": "number" }

POST /message/sendMedia/{instanceName}
Headers: Authorization: Bearer {EVOLUTION_API_KEY}
Request:  {
    "number": "string",
    "media": { "url": "string", "type": "image" | "video" | "audio" | "document" },
    "caption": "string"
}
Response: { "key": { "remoteJid": "string", "id": "string" } }
```

### 6.3 Webhook

```
POST /webhook/{instanceName}
Headers: Authorization: Bearer {EVOLUTION_API_KEY}
Body:    {
    "event": "messages.upsert" | "connection.update" | "presence.update",
    "data": { ... },
    "instance": "string"
}
Response: 200 OK
```

---

## 7. AI Agent (Python) — Internal

### 7.1 Kafka Consumer Topics

```
Consumer Group: ai-agent
Topics:
  - ai.agent.events
  - chat.events
```

### 7.2 Kafka Producer Topics

```
Produces to:
  - ai.response
  - chat.events
  - notification.events
```

### 7.3 Agent Types

```
AgentFactory.create("sales")     -> SalesAgent
AgentFactory.create("support")   -> SupportAgent
AgentFactory.create("marketing") -> MarketingAgent
```

### 7.4 Tool Registry

```
Available Tools:
  - search_products(query, tenant_id) -> Product[]
  - get_contact_info(contact_id, tenant_id) -> Contact
  - create_order(contact_id, items[], tenant_id) -> Order
  - get_order_status(order_id, tenant_id) -> OrderStatus
  - schedule_appointment(contact_id, datetime, tenant_id) -> Appointment
  - send_whatsapp_message(contact_id, message, tenant_id) -> MessageStatus
  - search_leads(criteria, tenant_id) -> Lead[]
  - update_pipeline_stage(conversation_id, stage_id, tenant_id) -> ConversationState
```

---

## 8. Marketing Agent (Python/CrewAI) — Internal

### 8.1 Kafka Consumer Topics

```
Consumer Group: marketing-agent
Topics:
  - campaign.events
  - ai.agent.events
  - lead.search.result
```

### 8.2 Kafka Producer Topics

```
Produces to:
  - campaign.events
  - lead.search.request
  - ai.agent.events
  - notification.events
```

### 8.3 CrewAI Agents

```
Agents:
  - ResearcherAgent: Discovers business data, market trends
  - ICPAgent: Defines Ideal Customer Profile
  - CopywriterAgent: Creates ad copy, email content
  - QualificationAgent: Scores and qualifies leads
  - CampaignManagerAgent: Orchestrates campaign execution
```

---

## 9. Marketing Worker (Java/Spring) — Internal

### 9.1 Kafka Consumer Topics

```
Consumer Group: marketing-worker
Topics:
  - campaign.events
  - lead.search.result
```

### 9.2 Campaign Execution Flow

```
1. Receive campaign.event (type: "send")
2. Query sending_list_contacts for the campaign
3. For each contact:
   a. Format message using MessageFormatterService
   b. Send via EvolutionService (WhatsApp)
   c. Log result in campaign_send_log
4. Update campaign status
5. Publish notification.event
```

---

## 10. Lead Generator (Python) — Port 8001

### 10.1 REST Endpoints

```
POST /api/leads/generate
Headers: Authorization: Bearer {JWT}
Request:  {
    "query": "string",
    "location": "string",
    "maxResults": "number",
    "tenantId": "number",
    "companyId": "number"
}
Response: {
    "leads": [
        {
            "id": "number",
            "name": "string",
            "phone": "string",
            "email": "string",
            "website": "string",
            "address": "string",
            "source": "apify",
            "score": "number"
        }
    ],
    "total": "number"
}

GET  /api/leads/{id}
Headers: Authorization: Bearer {JWT}
Response: LeadDetail

POST /api/leads/{id}/enrich
Headers: Authorization: Bearer {JWT}
Request:  { "fields": ["email", "phone", "social"] }
Response: EnrichedLead
```

### 10.2 Apify Integration

```
POST https://api.apify.com/v2/acts/{actorId}/run-sync-get-dataset-items
Headers: Authorization: Bearer {APIFY_TOKEN}
Request:  {
    "queries": "string",
    "maxResults": "number",
    "location": "string"
}
Response: BusinessData[]
```

---

## 11. Lead Scrapper Google (Python/Selenium) — Internal

### 11.1 Kafka Consumer Topics

```
Consumer Group: lead-scrapper-google
Topics:
  - lead.search.request
  - lead.scrapper.events
```

### 11.2 Kafka Producer Topics

```
Produces to:
  - lead.search.result
  - lead.scrapper.events
```

### 11.3 Scraping Flow

```
1. Receive lead.search.request
2. Launch headless browser (Chrome via Selenium)
3. Search Google for query + location
4. Extract business listings (name, phone, address, website)
5. Deduplicate against existing leads in MySQL
6. Store new leads
7. Publish lead.search.result
```

---

## 12. n8n (Workflow Automation) — Port 5678

### 12.1 Webhook Endpoints

```
POST /webhook/{workflowId}
Body:    { ...trigger data... }
Response: { "workflowId": "string", "executionId": "string" }

GET  /rest/workflows
Headers: Authorization: Bearer {N8N_API_KEY}
Response: Workflow[]

POST /rest/workflows/{id}/activate
Headers: Authorization: Bearer {N8N_API_KEY}
Response: { "active": true }
```

### 12.2 Pre-built Workflows

```
- sellerbot_system.json: Complete CRM automation
  Triggers: WhatsApp message, new contact, deal stage change
  Actions: Send message, update CRM, notify agent, create task
```

---

## 13. Frontend React (Next.js) — Port 3000

### 13.1 Page Routes

```
/                          -> Landing page
/login                     -> Login page
/register                  -> Registration page
/forgot-password           -> Password recovery
/reset-password/[id]       -> Password reset
/verify-email/[id]         -> Email verification
/oauth/callback/facebook   -> Facebook OAuth callback
/oauth/callback/google     -> Google OAuth callback

/dashboard                 -> Main dashboard
/dashboard/b2b-lead-generator -> B2B Lead Generator

/administracion/system     -> System Status (Docker services)
/administracion/planes     -> Plan management
/administracion/modules    -> Module management
/administracion/suscripciones -> Subscription management
/administracion/roles      -> Role management
/administracion/user       -> User management
/administracion/clientes   -> Customer management
/administracion/companies  -> Company management
/administracion/consumo    -> Consumption dashboard
/administracion/templates  -> Email templates
/administracion/tipos-chatbot -> Chatbot type config
/administracion/tipo-servicio -> Service type config
/administracion/tipo-dispositivo -> Device type config
/administracion/plantillas-verificacion -> Verification templates

/marketing/campaigns       -> Campaign list
/marketing/pipelines       -> Pipeline Kanban
/marketing/contacts        -> Contact list
/marketing/lists           -> Sending lists
/marketing/agents          -> AI Agent management
/marketing/ai-operation    -> AI Operation dashboard
/marketing/chatbots        -> Chatbot management
/marketing/channels        -> Channel configuration

/ventas/productos          -> Product list
/ventas/pedidos            -> Order list
/ventas/facturas           -> Invoice list
/ventas/cotizaciones       -> Quote list
/ventas/categorias         -> Category management
/ventas/notas-credito      -> Credit notes
/ventas/notas-debito       -> Debit notes

/contabilidad/balance-general    -> General balance
/contabilidad/balance-prueba    -> Trial balance
/contabilidad/libro-diario      -> Daily journal
/contabilidad/libro-mayor       -> General ledger
/contabilidad/estado-resultados -> Income statement
/contabilidad/centros-costo     -> Cost centers
/contabilidad/plan-cuentas      -> Chart of accounts
/contabilidad/comprobantes      -> Accounting vouchers

/hr/dashboard              -> HR dashboard
/hr/employees              -> Employee list
/hr/concepts               -> Payroll concepts
/hr/periods                -> Payroll periods
/hr/process                -> Payroll processing
/hr/receipts               -> Payroll receipts
/hr/novelties              -> Payroll novelties
/hr/config                 -> Payroll configuration
/hr/reports/cost-by-center -> Cost by cost center

/calendar                  -> Calendar view
/calendar/agenda           -> Agenda view
/calendar/configuracion    -> Calendar settings

/comunicaciones/canales    -> Channel management
/comunicaciones/conversaciones -> Conversations

/apps/ecommerce            -> E-commerce dashboard

/settings/integrations     -> Integration settings
/settings/channel-config   -> Channel configuration
/settings/roles            -> Role management
```

### 13.2 API Client

```typescript
// axiosInstance.ts
const axiosInstance = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8080',
});

// Request interceptor - add auth token and tenant headers
axiosInstance.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  const tenantId = localStorage.getItem('activeTenantId');
  const companyId = localStorage.getItem('activeCompanyId');
  
  if (token) config.headers.Authorization = `Bearer ${token}`;
  if (tenantId) config.headers['X-Tenant-Id'] = tenantId;
  if (companyId) config.headers['X-Company-Id'] = companyId;
  
  return config;
});
```

---

## 14. Portainer — Port 9000

### 14.1 Management Interface

```
URL: http://localhost:9000
Features:
  - Container management (start, stop, restart, remove)
  - Image management
  - Volume management
  - Network management
  - Stack management (docker-compose)
  - Console access to containers
  - Log viewing
  - Resource monitoring
```

---

## 15. Qdrant Vector Database — Port 6333

### 15.1 REST API

```
GET    /collections
Response: { "result": { "collections": [ { "name": "string" } ] } }

PUT    /collections/{name}
Request:  {
    "vectors": { "size": "number", "distance": "Cosine" },
    "optimizers_config": { "default_segment_number": "number" }
}
Response: { "result": true }

PUT    /collections/{name}/points
Request:  {
    "points": [
        { "id": "number", "vector": [...], "payload": { ... } }
    ]
}
Response: { "result": { "operation_id": "number" } }

POST   /collections/{name}/points/search
Request:  {
    "vector": [...],
    "limit": "number",
    "with_payload": true,
    "with_vector": false
}
Response: { "result": [ { "id": "number", "score": "number", "payload": { ... } } ] }

DELETE /collections/{name}
Response: { "result": true }
```

---

*🤖 Technical Writer — OWL | CloudFly AI Platform | 2026-06-07*
