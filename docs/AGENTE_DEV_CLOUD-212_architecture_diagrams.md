# CLOUD-212: marketingHistoryService — Architecture Diagrams

> **Document generated by:** 🤖 Technical Writer & Diagram Specialist  
> **Date:** 2025-07-19  
> **Ticket:** CLOUD-212 — Create marketingHistoryService for fetching history/tasks via REST  
> **Status:** ✅ **COMPLETE**

---

## 1. System Context Diagram

```mermaid
graph TB
    subgraph External["External Users"]
        U1["Marketing Manager"]
        U2["Sales Team"]
        U3["Admin"]
    end

    subgraph CloudFly["CloudFly AI Platform"]
        subgraph Frontend["Frontend Layer"]
            FE["frontend-react\n(Next.js, :3000)"]
        end

        subgraph Backend["Backend Layer"]
            BE["backend-api\n(Spring Boot WebFlux, :8080)"]
            CS["chat-socket-service\n(Socket.IO, :3001)"]
        end

        subgraph Data["Data Layer"]
            DB["MySQL\n(:3306)"]
            RD["Redis\n(:6379)"]
            KF["Kafka\n(:9092)"]
        end
    end

    subgraph External_Services["External Services"]
        MKT["marketing-agent\n(Python AI Agent)"]
    end

    U1 -->|"HTTPS"| FE
    U2 -->|"HTTPS"| FE
    U3 -->|"HTTPS"| FE

    FE -->|"REST API"| BE
    FE -->|"WebSocket"| CS

    BE -->|"R2DBC"| DB
    CS -->|"Consumer"| KF
    CS -->|"Pub/Sub"| RD

    MKT -->|"Produce"| KF

    style CloudFly fill:#f0f9ff,stroke:#0ea5e9
    style Frontend fill:#dbeafe,stroke:#3b82f6
    style Backend fill:#dcfce7,stroke:#22c55e
    style Data fill:#f3e8ff,stroke:#a855f7
    style External_Services fill:#fee2e2,stroke:#ef4444
```

---

## 2. Component Architecture Diagram

```mermaid
graph TB
    subgraph Page["page.tsx (MarketingLiveDashboardPage)"]
        P1["ConnectionChip"]
        P2["StatCard (×4)"]
        P3["AgentFlowGraph"]
        P4["LiveAgentCard (×N)"]
        P5["MarketingHistoryTimeline"]
    end

    subgraph Services["Services Layer"]
        S1["marketingHistoryService"]
        S2["useMarketingAgentsSocket"]
    end

    subgraph State["State Management"]
        ST1["agents: MarketingAgent[]"]
        ST2["connections: AgentConnection[]"]
        ST3["events: MarketingActionEvent[]"]
        ST4["connectionStatus: 'connected' | 'disconnected' | 'reconnecting'"]
    end

    P1 -->|"reads"| ST4
    P2 -->|"reads"| ST1
    P3 -->|"reads"| ST1
    P3 -->|"reads"| ST2
    P4 -->|"reads"| ST1
    P5 -->|"reads"| ST3

    S1 -->|"fetches"| ST1
    S1 -->|"fetches"| ST2
    S2 -->|"updates"| ST1
    S2 -->|"updates"| ST2
    S2 -->|"updates"| ST3
    S2 -->|"updates"| ST4

    style Page fill:#dbeafe,stroke:#3b82f6
    style Services fill:#fef3c7,stroke:#f59e0b
    style State fill:#dcfce7,stroke:#22c55e
```

---

## 3. Service Method Flow Diagram

```mermaid
flowchart TD
    subgraph getLiveAgents["getLiveAgents(tenantId, companyId?, signal?)"]
        LA1["Build URLSearchParams"] --> LA2["Append tenantId"]
        LA2 --> LA3{"companyId?"}
        LA3 -->|Yes| LA4["Append companyId"]
        LA3 -->|No| LA5["axiosInstance.get"]
        LA4 --> LA5
        LA5 --> LA6{"Response?"}
        LA6 -->|Success| LA7["Return { agents, connections }"]
        LA6 -->|AbortError| LA8["Return { agents: [], connections: [] }"]
        LA6 -->|Other Error| LA9["console.error"] --> LA8
    end

    subgraph getActionHistory["getActionHistory(tenantId, limit=50, page=0, companyId?, signal?)"]
        AH1["Build URLSearchParams"] --> AH2["Append tenantId, limit, page"]
        AH2 --> AH3{"companyId?"}
        AH3 -->|Yes| AH4["Append companyId"]
        AH3 -->|No| AH5["axiosInstance.get"]
        AH4 --> AH5
        AH5 --> AH6{"Response?"}
        AH6 -->|Success| AH7["Return MarketingHistoryResponse"]
        AH6 -->|AbortError| AH8["Return { events: [], total: 0, hasMore: false }"]
        AH6 -->|Other Error| AH9["console.error"] --> AH8
    end

    subgraph getAgentConnections["getAgentConnections(tenantId, signal?)"]
        AC1["axiosInstance.get"] --> AC2{"Response?"}
        AC2 -->|Success| AC3["Return AgentConnection[]"]
        AC2 -->|AbortError| AC4["Return []"]
        AC2 -->|Other Error| AC5["console.error"] --> AC4
    end

    subgraph getAgentTasks["getAgentTasks(agentId, signal?)"]
        AT1["axiosInstance.get"] --> AT2{"Response?"}
        AT2 -->|Success| AT3["Return MarketingActionEvent[]"]
        AT2 -->|AbortError| AT4["Return []"]
        AT2 -->|Other Error| AT5["console.error"] --> AT4
    end
```

---

## 4. WebSocket Event Flow Diagram

```mermaid
sequenceDiagram
    autonumber
    participant FE as Frontend (page.tsx)
    participant Hook as useMarketingAgentsSocket
    participant Socket as Socket.IO Client
    participant Server as chat-socket-service
    participant Kafka as Kafka Consumer

    rect rgb(220, 252, 231)
        Note over FE,Server: Connection Phase
        FE->>Hook: Mount component
        Hook->>Socket: connect()
        Socket-->>Hook: connect event
        Hook->>Server: emit('subscribe-marketing', { tenantId, companyId })
        Server-->>Hook: emit('subscribed-marketing', { room })
        Hook-->>FE: isConnected = true
    end

    rect rgb(219, 234, 254)
        Note over FE,Server: Real-time Updates
        Kafka->>Server: marketing-agent-batch-update
        Server->>Hook: emit('marketing-agent-batch-update', payload)
        Hook->>Hook: setAgents(payload.agents)
        Hook->>Hook: setConnections(payload.connections)
        Hook-->>FE: Updated state

        Kafka->>Server: marketing-agent-status-update
        Server->>Hook: emit('marketing-agent-status-update', payload)
        Hook->>Hook: Patch single agent
        Hook-->>FE: Updated agents

        Kafka->>Server: marketing-action-event
        Server->>Hook: emit('marketing-action-event', payload)
        Hook->>Hook: Append event (dedup, max 50)
        Hook-->>FE: Updated events
    end

    rect rgb(254, 243, 199)
        Note over FE,Server: Reconnection Phase
        Hook->>Hook: reconnect() called
        Hook->>Socket: disconnect()
        Hook->>Socket: connect()
        Socket-->>Hook: connect event
        Hook->>Server: emit('subscribe-marketing', { tenantId, companyId })
        Server-->>Hook: emit('subscribed-marketing', { room })
        Hook-->>FE: isConnected = true
    end
```

---

## 5. Database Schema Diagram

```mermaid
erDiagram
    global_agent {
        bigint id PK
        varchar name "Agent code name"
        varchar description "Agent description"
        boolean is_active "Soft delete flag"
        timestamp created_at
        timestamp updated_at
    }

    tenant_agent_config {
        bigint id PK
        bigint tenant_id FK "References tenant"
        bigint global_agent_id FK "References global_agent"
        varchar display_name "Tenant-specific display name"
        boolean is_active "Tenant-specific activation"
        json config "Tenant-specific configuration"
        timestamp created_at
    }

    workflow_execution_log {
        bigint id PK
        bigint workflow_id "Agent/workflow identifier"
        bigint tenant_id "Tenant scope"
        bigint company_id "Company scope"
        varchar status "SUCCESS|FAILED|FILTERED"
        bigint execution_time_ms "Performance metric"
        text error_message "Error details if failed"
        json metadata "Additional context"
        timestamp created_at
    }

    global_agent ||--o{ tenant_agent_config : "1:N (tenant-specific configs)"
    global_agent ||--o{ workflow_execution_log : "1:N (execution history)"
    tenant_agent_config }o--|| global_agent : "N:1 (global definition)"
```

---

## 6. State Machine Diagram

```mermaid
stateDiagram-v2
    [*] --> Initializing

    Initializing --> LoadingREST: Component mounts
    LoadingREST --> RESTLoaded: REST call succeeds
    LoadingREST --> RESTError: REST call fails (non-abort)
    RESTError --> RESTLoaded: Use empty defaults

    RESTLoaded --> WebSocketConnecting: Socket connects
    WebSocketConnecting --> Connected: Socket connected + subscribed
    WebSocketConnecting --> ConnectionFailed: Socket connection failed

    Connected --> Reconnecting: User clicks reconnect OR socket disconnects
    Reconnecting --> Connected: Socket reconnects
    Reconnecting --> Disconnected: Reconnect failed

    Connected --> Disconnected: Socket disconnects unexpectedly
    Disconnected --> Reconnecting: Auto-reconnect attempt

    Connected --> Unmounting: User navigates away
    Disconnected --> Unmounting: User navigates away
    Reconnecting --> Unmounting: User navigates away
    Unmounting --> [*]: AbortController.abort() + socket.off()

    note right of Connected: Real-time updates flowing\nagents[], connections[], events[]
    note right of RESTLoaded: Initial data loaded\nWebSocket not yet connected
    note right of Unmounting: Cleanup:\n- AbortController.abort()\n- socket.off(all listeners)\n- unsubscribe-marketing
```

---

## 7. Error Handling Flow Diagram

```mermaid
flowchart TD
    A[HTTP Request] --> B{Request Result}

    B -->|Success| C[Return typed response]
    B -->|Error| D{isAbortError?}

    D -->|AbortError.name| E[Silent return empty default]
    D -->|CanceledError.name| E
    D -->|ERR_CANCELED.code| E

    D -->|Other Error| F[console.error]
    F --> G[Return empty default]

    E --> H[No error state set]
    G --> I[Set error state for UI]

    style C fill:#dcfce7,stroke:#22c55e
    style E fill:#dbeafe,stroke:#3b82f6
    style I fill:#fee2e2,stroke:#ef4444
```

---

## 8. AbortController Lifecycle Diagram

```mermaid
sequenceDiagram
    autonumber
    participant Comp as page.tsx
    participant Ctrl as AbortController
    participant Svc as marketingHistoryService
    participant Axios as axiosInstance

    rect rgb(220, 252, 231)
        Note over Comp,Axios: Mount Phase
        Comp->>Ctrl: new AbortController()
        Comp->>Svc: getActionHistory(tenantId, 50, 0, companyId, ctrl.signal)
        Svc->>Axios: axios.get(url, { signal: ctrl.signal })
        Axios-->>Svc: Response data
        Svc-->>Comp: MarketingHistoryResponse
    end

    rect rgb(254, 243, 199)
        Note over Comp,Axios: Unmount Phase (Request In-Flight)
        Comp->>Ctrl: ctrl.abort()
        Ctrl->>Axios: AbortSignal triggered
        Axios--x>Svc: DOMException: AbortError
        Svc->>Svc: isAbortError(error) === true
        Svc-->>Comp: { events: [], total: 0, hasMore: false }
        Note over Comp: NO state update (component unmounted)
    end

    rect rgb(219, 234, 254)
        Note over Comp,Axios: Unmount Phase (Request Completed)
        Comp->>Ctrl: ctrl.abort()
        Note over Comp: No effect (request already completed)
    end
```

---

## 9. Deployment Topology Diagram

```mermaid
graph TB
    subgraph Internet["Internet"]
        Browser["User Browser"]
    end

    subgraph VPS["VPS (api.cloudfly.com.co)"]
        subgraph Traefik["Traefik (:80/:443)"]
            T1["dashboard.cloudfly.com.co"]
            T2["api.cloudfly.com.co"]
            T3["chat.cloudfly.com.co"]
        end

        subgraph AppNet["app-net"]
            FE["frontend-react\n:3000"]
            BE["backend-api\n:8080"]
            CS["chat-socket-service\n:3001"]
            DB["mysql\n:3306"]
            RD["redis_server\n:6379"]
        end

        subgraph KafkaNet["kafka-net"]
            KF["kafka\n:9092"]
            ZK["zookeeper\n:2181"]
        end
    end

    Browser -->|"HTTPS"| T1
    Browser -->|"HTTPS"| T2
    Browser -->|"WSS"| T3

    T1 --> FE
    T2 --> BE
    T3 --> CS

    FE -->|"REST"| BE
    FE -->|"WebSocket"| CS

    BE -->|"R2DBC"| DB
    CS -->|"Consumer"| KF
    CS -->|"Pub/Sub"| RD

    BE -.->|"also on"| KafkaNet
    CS -.->|"also on"| KafkaNet
    DB -.->|"also on"| KafkaNet
```

---

## 10. Type Mapping Diagram

```mermaid
flowchart LR
    subgraph Java["Java DTOs (Backend)"]
        J1["MarketingAgentDto"]
        J2["AgentConnectionDto"]
        J3["MarketingActionEventDto"]
    end

    subgraph JSON["JSON Response"]
        JS1["camelCase fields"]
    end

    subgraph TypeScript["TypeScript Interfaces (Frontend)"]
        T1["MarketingAgent"]
        T2["AgentConnection"]
        T3["MarketingActionEvent"]
    end

    J1 -->|"Jackson\nserialization"| JS1
    J2 -->|"Jackson\nserialization"| JS1
    J3 -->|"Jackson\nserialization"| JS1

    JS1 -->|"Axios\nresponse.data"| T1
    JS1 -->|"Axios\nresponse.data"| T2
    JS1 -->|"Axios\nresponse.data"| T3

    style Java fill:#dcfce7,stroke:#22c55e
    style JSON fill:#fef3c7,stroke:#f59e0b
    style TypeScript fill:#dbeafe,stroke:#3b82f6
```

---

## 11. Test Coverage Diagram

```mermaid
pie showData
    title Unit Test Distribution (34 tests total)
    "Exports & Structure" : 6
    "getLiveAgents" : 6
    "getActionHistory" : 7
    "getAgentConnections" : 4
    "getAgentTasks" : 4
    "AbortError Detection" : 4
    "Backward Compatibility" : 4
```

---

## 12. File Dependency Diagram

```mermaid
graph TD
    PAGE["page.tsx"] -->|"imports"| SVC["marketingHistoryService.ts"]
    PAGE -->|"imports"| HOOK["useMarketingAgentsSocket.ts"]
    PAGE -->|"imports"| TYPES["aiMarketing.ts"]
    PAGE -->|"imports"| COMP["UI Components"]

    SVC -->|"imports"| AXIOS["axiosInstance.ts"]
    SVC -->|"imports"| TYPES

    HOOK -->|"imports"| TYPES
    HOOK -->|"uses"| SOCKET["SocketContext"]

    CTRL["MarketingHistoryController.java"] -->|"uses"| REPO1["GlobalAgentRepository"]
    CTRL -->|"uses"| REPO2["TenantAgentConfigRepository"]
    CTRL -->|"uses"| REPO3["WorkflowExecutionLogRepository"]

    MHAND["marketingHandler.js"] -->|"uses"| LOGGER["utils/logger"]

    style PAGE fill:#dbeafe,stroke:#3b82f6
    style SVC fill:#fef3c7,stroke:#f59e0b
    style HOOK fill:#fef3c7,stroke:#f59e0b
    style CTRL fill:#dcfce7,stroke:#22c55e
    style MHAND fill:#dcfce7,stroke:#22c55e
```

---

*🤖 Technical Writer & Diagram Specialist — CLOUD-212 Architecture Diagrams Complete*
