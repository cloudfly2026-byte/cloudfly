# CLOUD-320: Architecture Diagrams

> **Generated by**: OWL Technical Writer  
> **Date**: 2026-06-07

---

## 1. System Architecture Overview

```mermaid
flowchart TB
    subgraph External["External Systems"]
        WA["WhatsApp Server"]
        WOMPI["Wompi Payment Gateway"]
        DDG["DuckDuckGo API"]
        APIFY["Apify Scraping"]
    end

    subgraph Client["Client Layer"]
        FE["frontend-react :3000\n(Next.js Dashboard)"]
        PORTAINER["portainer :9000\n(Container Management)"]
    end

    subgraph Core["Core Platform"]
        BE["backend-api :8080\n(Spring Boot WebFlux)"]
        EV["evolution-api :8082\n(WhatsApp Integration)"]
    end

    subgraph Messaging["Event Bus"]
        KAFKA["kafka :9092\n(confluentinc/cp-kafka:7.4.0)"]
        ZK["zookeeper :2181"]
    end

    subgraph Business["Business Services"]
        BILLING["billing-service :8086\n(Go)"]
        SCHEDULER["scheduler-service :8085\n(Java/Spring)"]
        NOTIFY["notification-service\n(Java/Spring)"]
        CHAT["chat-socket-service :3001\n(Node.js/Socket.IO)"]
        AI["ai-agent\n(Python 3.13)"]
        VECTOR["ai-vector-worker\n(Python 3.13)"]
        MA["marketing-agent\n(Python 3.13/CrewAI)"]
        MW["marketing-worker\n(Java/Spring)"]
    end

    subgraph Integration["Integration Services"]
        LG["lead-generator :8001\n(Python/Apify)"]
        SCRAP["lead-scrapper-google\n(Python/Selenium)"]
        N8N["n8n :5678\n(Workflow Automation)"]
    end

    subgraph Data["Data Layer"]
        MYSQL["mysql :3306\n(cloud_master DB)"]
        REDIS["redis :6379\n(Cache/Sessions)"]
        PG["postgresql :5432\n(chatbotdb/pgvector)"]
        QDRANT["qdrant :6333\n(Vector Search)"]
    end

    %% External connections
    WA --> EV
    EV --> WA
    BILLING --> WOMPI
    SCRAP --> DDG
    LG --> APIFY

    %% Client to core
    FE --> BE
    FE --> CHAT

    %% Core to messaging
    BE --> KAFKA
    MA --> KAFKA
    SCHEDULER --> KAFKA

    %% Core to data
    BE --> MYSQL
    BE --> REDIS
    EV --> PG
    EV --> REDIS

    %% Messaging to business
    KAFKA --> BILLING
    KAFKA --> NOTIFY
    KAFKA --> CHAT
    KAFKA --> AI
    KAFKA --> VECTOR
    KAFKA --> MA
    KAFKA --> MW
    KAFKA --> SCRAP

    %% Business to data
    BILLING --> MYSQL
    SCHEDULER --> MYSQL
    NOTIFY --> MYSQL
    CHAT --> MYSQL
    CHAT --> REDIS
    AI --> MYSQL
    AI --> REDIS
    VECTOR --> MYSQL
    VECTOR --> QDRANT
    MA --> MYSQL
    MW --> MYSQL
    MW --> EV
    LG --> MYSQL
    SCRAP --> MYSQL

    %% Core dependencies
    BE --> EV
    BE --> MYSQL
    BE --> KAFKA
    EV --> REDIS

    style External fill:#fff3e0,stroke:#e65100
    style Client fill:#fce4ec,stroke:#880e4f
    style Core fill:#f3e5f5,stroke:#4a148c
    style Messaging fill:#e8eaf6,stroke:#283593
    style Business fill:#e8f5e9,stroke:#1b5e20
    style Integration fill:#fff8e1,stroke:#f57f17
    style Data fill:#e0f2f1,stroke:#004d40
```

---

## 2. Docker Network Topology

```mermaid
flowchart TB
    subgraph default_net["default (bridge)"]
        MYSQL["mysql :3306"]
    end

    subgraph kafka_net["kafka-net (bridge)"]
        MYSQL2["mysql"]
        ZK["zookeeper :2181"]
        KAFKA["kafka :9092/9093"]
        BE2["backend-api :8080"]
        BILLING2["billing-service :8086"]
        SCHEDULER2["scheduler-service :8085"]
        NOTIFY2["notification-service"]
        CHAT2["chat-socket-service :3001"]
        AI2["ai-agent"]
        VECTOR2["ai-vector-worker"]
        MA2["marketing-agent"]
        MW2["marketing-worker"]
        SCRAP2["lead-scrapper-google"]
    end

    subgraph app_net["app-net (bridge)"]
        MYSQL3["mysql"]
        BE3["backend-api :8080"]
        BILLING3["billing-service :8086"]
        SCHEDULER3["scheduler-service :8085"]
        NOTIFY3["notification-service"]
        CHAT3["chat-socket-service :3001"]
        AI3["ai-agent"]
        VECTOR3["ai-vector-worker"]
        MA3["marketing-agent"]
        MW3["marketing-worker"]
        EV3["evolution-api :8082"]
        REDIS3["redis :6379"]
        PG3["postgresql :5432"]
        LG3["lead-generator :8001"]
        SCRAP3["lead-scrapper-google"]
        N8N3["n8n :5678"]
        FE3["frontend-react :3000"]
        PORTAINER3["portainer :9000"]
        QDRANT3["qdrant :6333"]
    end

    %% Cross-network connections
    MYSQL --- MYSQL2
    MYSQL2 --- MYSQL3
    BE2 --- BE3
    BILLING2 --- BILLING3
    SCHEDULER2 --- SCHEDULER3
    NOTIFY2 --- NOTIFY3
    CHAT2 --- CHAT3
    AI2 --- AI3
    VECTOR2 --- VECTOR3
    MA2 --- MA3
    MW2 --- MW3
    SCRAP2 --- SCRAP3

    style default_net fill:#f5f5f5,stroke:#9e9e9e
    style kafka_net fill:#e8eaf6,stroke:#3f51b5
    style app_net fill:#e0f2f1,stroke:#009688
```

---

## 3. Service Boot Sequence

```mermaid
sequenceDiagram
    participant User as Developer
    participant DC as docker-compose
    participant P1 as Phase 1 Services
    participant P2 as Phase 2 Services
    participant P3 as Phase 3 Services
    participant P4 as Phase 4 Services
    participant P5 as Phase 5 Services

    User->>DC: docker-compose up -d
    DC->>P1: Start infrastructure
    Note over P1: mysql, zookeeper, redis, postgresql, qdrant
    P1-->>DC: ✅ Infrastructure ready
    DC->>P2: Start core platform
    Note over P2: kafka, backend-api, evolution-api
    P2-->>DC: ✅ Core ready
    DC->>P3: Start business services
    Note over P3: billing, scheduler, notification, chat, ai-agent, ai-vector, marketing-agent, marketing-worker
    P3-->>DC: ✅ Business ready
    DC->>P4: Start integrations
    Note over P4: lead-generator, lead-scrapper-google, n8n
    P4-->>DC: ✅ Integrations ready
    DC->>P5: Start frontend & management
    Note over P5: frontend-react, portainer
    P5-->>DC: ✅ Frontend ready
    DC-->>User: 🎉 All 20 services Up
```

---

## 4. Database ERD (Simplified)

```mermaid
erdiagram
    Tenant {
        int tenant_id PK
        string name
        string plan
        datetime created_at
    }
    Company {
        int company_id PK
        int tenant_id FK
        string name
        string nit
    }
    User {
        int user_id PK
        int tenant_id FK
        int company_id FK
        string email
        string password_hash
        string role
    }
    Contact {
        int contact_id PK
        int company_id FK
        string name
        string phone
        string email
    }
    Product {
        int product_id PK
        int company_id FK
        string name
        decimal price
        int stock
    }
    Order {
        int order_id PK
        int contact_id FK
        int company_id FK
        decimal total
        string status
    }
    Invoice {
        int invoice_id PK
        int order_id FK
        string dian_status
        string cufe
    }
    Pipeline {
        int pipeline_id PK
        int company_id FK
        string name
    }
    PipelineStage {
        int stage_id PK
        int pipeline_id FK
        string name
        int order
    }
    Conversation {
        int conversation_id PK
        int contact_id FK
        int pipeline_stage_id FK
        string status
    }
    Message {
        int message_id PK
        int conversation_id FK
        string direction
        string content
        string platform
    }
    Campaign {
        int campaign_id PK
        int company_id FK
        string name
        string channel
        string status
    }
    Subscription {
        int subscription_id PK
        int tenant_id FK
        int plan_id FK
        string status
        date next_billing
    }
    Workflow {
        int workflow_id PK
        int company_id FK
        string name
        string trigger_type
        json definition
    }
    Appointment {
        int appointment_id PK
        int contact_id FK
        int user_id FK
        datetime start_time
        string status
    }
    Employee {
        int employee_id PK
        int company_id FK
        string name
        string contract_type
        decimal salary
    }
    PayrollReceipt {
        int receipt_id PK
        int employee_id FK
        int period_id FK
        decimal total_devengado
        decimal total_deducciones
    }
    Lead {
        int lead_id PK
        int company_id FK
        string source
        string name
        string phone
        string status
    }

    Tenant ||--o{ Company : has
    Tenant ||--o{ User : has
    Company ||--o{ Contact : has
    Company ||--o{ Product : has
    Company ||--o{ Order : has
    Company ||--o{ Pipeline : has
    Company ||--o{ Campaign : has
    Company ||--o{ Employee : has
    Company ||--o{ Lead : has
    Contact ||--o{ Order : places
    Contact ||--o{ Conversation : participates
    Order ||--o| Invoice : generates
    Pipeline ||--o{ PipelineStage : has
    Conversation ||--o{ Message : contains
    Campaign ||--o{ Message : sends
    Subscription ||--|| Tenant : belongs_to
    Appointment ||--|| Contact : with
    Appointment ||--|| User : assigned_to
    PayrollReceipt ||--|| Employee : for
    PayrollReceipt ||--|| PayrollPeriod : in
```

---

## 5. Kafka Event Flow

```mermaid
flowchart LR
    subgraph Producers
        BE["backend-api"]
        MA["marketing-agent"]
        SCH["scheduler-service"]
    end

    subgraph Topics
        T1["billing.events"]
        T2["notification.events"]
        T3["campaign.events"]
        T4["chat.events"]
        T5["ai.agent.events"]
        T6["lead.scrapper.events"]
        T7["payroll.events"]
    end

    subgraph Consumers
        BILL["billing-service"]
        NOTIF["notification-service"]
        CHAT["chat-socket-service"]
        AI["ai-agent"]
        VEC["ai-vector-worker"]
        SCR["lead-scrapper-google"]
        MW["marketing-worker"]
    end

    BE --> T1
    BE --> T2
    BE --> T3
    BE --> T4
    MA --> T3
    MA --> T5
    SCH --> T7

    T1 --> BILL
    T2 --> NOTIF
    T3 --> MW
    T4 --> CHAT
    T5 --> AI
    T5 --> VEC
    T6 --> SCR
    T7 --> NOTIF
```

---

## 6. Multi-Tenant Data Isolation

```mermaid
flowchart TB
    subgraph Request["API Request"]
        JWT["JWT Token\n(tenant_id, company_id)"]
    end

    subgraph Backend["backend-api"]
        FILTER["Tenant Filter\nMiddleware"]
        SERVICE["Business Service"]
    end

    subgraph Database["MySQL cloud_master"]
        TENANT["tenant_id column\nin every table"]
    end

    JWT --> FILTER
    FILTER --> SERVICE
    SERVICE --> TENANT
    TENANT -->|"Only rows matching\ntenant_id + company_id"| RESULT["Filtered Results"]
```

---

## 7. WhatsApp Integration Flow

```mermaid
sequenceDiagram
    participant User as WhatsApp User
    participant WA as WhatsApp Server
    participant EV as evolution-api :8082
    participant BE as backend-api :8080
    participant KAF as kafka :9092
    participant CS as chat-socket-service :3001
    participant AI as ai-agent
    participant DB as mysql :3306

    User->>WA: Send message
    WA->>EV: Webhook POST /webhook
    EV->>BE: POST /webhook/evolution
    BE->>DB: INSERT INTO omni_channel_messages
    BE->>KAF: Publish chat.event
    KAF->>CS: Consume chat.event
    CS->>CS: Broadcast via Socket.IO
    KAF->>AI: Consume ai.agent.events
    AI->>AI: Process with LLM
    AI->>KAF: Publish ai.response
    KAF->>CS: Forward response
    CS->>BE: POST /evolution/send
    BE->>EV: POST /message/sendText
    EV->>WA: Send message via API
    WA->>User: Deliver response
```

---

## 8. Billing Cycle Flow

```mermaid
sequenceDiagram
    participant SCH as scheduler-service :8085
    participant DB as mysql :3306
    participant KAF as kafka :9092
    participant BILL as billing-service :8086
    participant WOMPI as Wompi API
    participant NOTIF as notification-service

    SCH->>DB: SELECT subscriptions WHERE next_billing = TODAY
    SCH->>KAF: Publish billing.event
    KAF->>BILL: Consume billing.event
    BILL->>WOMPI: POST /transactions
    WOMPI-->>BILL: Transaction result
    BILL->>DB: UPDATE subscription status
    BILL->>KAF: Publish notification.event
    KAF->>NOTIF: Consume notification.event
    NOTIF->>NOTIF: Send email to customer
```

---

## 9. Lead Scraping Pipeline

```mermaid
flowchart TB
    subgraph Trigger["Trigger"]
        MA["marketing-agent\n(CrewAI)"]
    end

    subgraph Kafka["Event Bus"]
        REQ["lead.search.request"]
        RES["lead.search.result"]
    end

    subgraph Scraping["Scraping Workers"]
        SCRAP1["lead-scrapper-google\n(Worker 1)"]
        SCRAP2["lead-scrapper-google\n(Worker 2)"]
        SCRAP3["lead-scrapper-google\n(Worker 3)"]
    end

    subgraph Enrichment["Enrichment"]
        LG["lead-generator\n(Apify)"]
    end

    subgraph Storage["Storage"]
        DB["mysql\nleads table"]
    end

    MA --> REQ
    REQ --> SCRAP1
    REQ --> SCRAP2
    REQ --> SCRAP3
    SCRAP1 --> DB
    SCRAP2 --> DB
    SCRAP3 --> DB
    DB --> LG
    LG --> DB
    DB --> RES
    RES --> MA
```

---

## 10. AI Agent Architecture

```mermaid
flowchart TB
    subgraph Input["Input Layer"]
        KAF_IN["Kafka Consumer\n(ai.agent.events)"]
    end

    subgraph Processing["AI Processing"]
        AGENT["agent_factory.py\nCreates agent based on type"]
        SALES["sales_agent.py"]
        SUPPORT["support_agent.py"]
        LLM["LLM Service\n(OpenRouter/OpenAI)"]
        TOOLS["tool_registry.py\nAvailable tools"]
    end

    subgraph Data["Data Access"]
        MYSQL["mysql_client.py\nMySQL queries"]
        REDIS["redis_client.py\nCache access"]
    end

    subgraph Output["Output Layer"]
        KAF_OUT["Kafka Producer\n(ai.response)"]
        DB_OUT["Store conversation\nin MySQL"]
    end

    KAF_IN --> AGENT
    AGENT --> SALES
    AGENT --> SUPPORT
    SALES --> LLM
    SUPPORT --> LLM
    LLM --> TOOLS
    TOOLS --> MYSQL
    TOOLS --> REDIS
    LLM --> KAF_OUT
    LLM --> DB_OUT
```

---

*🤖 Technical Writer — OWL | CloudFly AI Platform | 2026-06-07*
