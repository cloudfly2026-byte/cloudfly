# CLOUD-212: marketingHistoryService — API Contracts

> **Document generated by:** 🤖 Technical Writer & Diagram Specialist  
> **Date:** 2025-07-19  
> **Ticket:** CLOUD-212 — Create marketingHistoryService for fetching history/tasks via REST  
> **Status:** ✅ **COMPLETE**

---

## 1. REST API Endpoints

### 1.1 GET /api/v1/marketing/agents/live-status

Returns all active agents with their current status and connections for the initial dashboard load or WebSocket reconnection fallback.

**Query Parameters:**

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `tenantId` | `Long` | ✅ Yes | — | Tenant identifier |
| `companyId` | `Long` | No | `null` | Optional company identifier for scoped queries |

**Response (200 OK):**

```json
{
  "agents": [
    {
      "id": "1",
      "name": "Investigador de Mercado",
      "status": "idle",
      "currentTask": "Waiting for tasks",
      "lastActivity": "2025-07-19T10:30:00",
      "avatarUrl": null
    }
  ],
  "connections": [
    {
      "from": "1",
      "to": "2",
      "label": "feeds into"
    }
  ]
}
```

**Error Responses:**

| Status | Description |
|---|---|
| 401 | Unauthorized — Invalid or missing JWT |
| 500 | Internal server error |

---

### 1.2 GET /api/v1/marketing/agents/history

Returns agent status, connections, and recent action history events for the timeline component.

**Query Parameters:**

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `tenantId` | `Long` | ✅ Yes | — | Tenant identifier |
| `limit` | `int` | No | `50` | Maximum number of events to return |
| `page` | `int` | No | `0` | Page number for pagination |
| `companyId` | `Long` | No | `null` | Optional company identifier for scoped queries |

**Response (200 OK):**

```json
{
  "agents": [
    {
      "id": "1",
      "name": "Investigador de Mercado",
      "status": "idle",
      "currentTask": "Waiting for tasks",
      "lastActivity": "2025-07-19T10:30:00",
      "avatarUrl": null
    }
  ],
  "connections": [
    {
      "from": "1",
      "to": "2",
      "label": "feeds into"
    }
  ],
  "recentEvents": [
    {
      "id": "101",
      "agentId": "1",
      "agentName": "Agent-1",
      "actionType": "WORKFLOW_EXECUTION",
      "description": "Workflow executed successfully",
      "timestamp": "2025-07-19T10:30:00",
      "metadata": {
        "status": "SUCCESS",
        "executionTimeMs": 1500,
        "companyId": 1
      }
    }
  ],
  "generatedAt": "2025-07-19T10:30:00"
}
```

**Error Responses:**

| Status | Description |
|---|---|
| 401 | Unauthorized — Invalid or missing JWT |
| 500 | Internal server error |

---

### 1.3 GET /api/v1/marketing/agents/connections

Returns directed agent connections for the flow graph component.

**Query Parameters:**

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `tenantId` | `Long` | ✅ Yes | — | Tenant identifier |

**Response (200 OK):**

```json
[
  {
    "from": "1",
    "to": "2",
    "label": "feeds into"
  },
  {
    "from": "2",
    "to": "3",
    "label": "feeds into"
  }
]
```

**Error Responses:**

| Status | Description |
|---|---|
| 401 | Unauthorized — Invalid or missing JWT |
| 500 | Internal server error |

---

### 1.4 GET /api/v1/marketing/agents/{agentId}/tasks

Returns recent action events (tasks) for a specific agent.

**Path Parameters:**

| Parameter | Type | Required | Description |
|---|---|---|---|
| `agentId` | `string` | ✅ Yes | Unique agent identifier |

**Response (200 OK):**

```json
[
  {
    "id": "101",
    "agentId": "1",
    "agentName": "Agent-1",
    "actionType": "WORKFLOW_EXECUTION",
    "description": "Workflow executed successfully",
    "timestamp": "2025-07-19T10:30:00",
    "metadata": {
      "status": "SUCCESS",
      "executionTimeMs": 1500,
      "companyId": 1
    }
  }
]
```

**Error Responses:**

| Status | Description |
|---|---|
| 401 | Unauthorized — Invalid or missing JWT |
| 404 | Agent not found |
| 500 | Internal server error |

---

## 2. TypeScript Service Interface

### 2.1 marketingHistoryService

```typescript
// File: frontend_new/src/services/marketing/marketingHistoryService.ts

import axiosInstance from '@/utils/axiosInstance'
import type {
  MarketingAgent,
  AgentConnection,
  MarketingHistoryResponse,
  MarketingActionEvent
} from '@/types/marketing/aiMarketing'

const API_URL = `${process.env.NEXT_PUBLIC_API_URL}/api/v1/marketing`

export const marketingHistoryService = {

  /**
   * Fetch current agent states (for initial load / reconnection fallback).
   * GET /api/v1/marketing/agents/live-status?tenantId={}&companyId={}
   */
  async getLiveAgents(
    tenantId: number,
    companyId?: number,
    signal?: AbortSignal
  ): Promise<{ agents: MarketingAgent[]; connections: AgentConnection[] }> {
    // Implementation...
  },

  /**
   * Fetch action history for the timeline.
   * GET /api/v1/marketing/agents/history?tenantId={}&limit={}&page={}&companyId={}
   */
  async getActionHistory(
    tenantId: number,
    limit = 50,
    page = 0,
    companyId?: number,
    signal?: AbortSignal
  ): Promise<MarketingHistoryResponse> {
    // Implementation...
  },

  /**
   * Fetch agent connections for the flow graph.
   * GET /api/v1/marketing/agents/connections?tenantId={}
   */
  async getAgentConnections(
    tenantId: number,
    signal?: AbortSignal
  ): Promise<AgentConnection[]> {
    // Implementation...
  },

  /**
   * Fetch tasks for a specific agent.
   * GET /api/v1/marketing/agents/{agentId}/tasks
   */
  async getAgentTasks(
    agentId: string,
    signal?: AbortSignal
  ): Promise<MarketingActionEvent[]> {
    // Implementation...
  }
}
```

---

## 3. TypeScript Type Definitions

### 3.1 Core Types

```typescript
// File: frontend_new/src/types/marketing/aiMarketing.ts

/**
 * Union type for all possible real-time statuses of a marketing agent.
 */
export type AgentStatus = 'idle' | 'working' | 'waiting' | 'error' | 'completed'

/**
 * A single marketing agent with live status information.
 */
export interface MarketingAgent {
  id: string
  name: string
  displayName: string
  role: string
  status: AgentStatus
  currentTask: string | null
  taskStartedAt: string | null
  lastActivity: string
  avatar?: string
  color: string
  position: { x: number; y: number }
}

/**
 * Directed connection between two agents in the flow graph.
 */
export interface AgentConnection {
  id: string
  sourceAgentId: string
  targetAgentId: string
  label: string
  dataFlow: 'leads' | 'analysis' | 'messages' | 'context'
  active: boolean
}

/**
 * A marketing action event for the history timeline.
 */
export interface MarketingActionEvent {
  id: string
  type: 'lead_search_started' | 'lead_search_completed' | 'campaign_created' |
        'message_sent' | 'analysis_completed' | 'crew_kickoff' | 'flow_transition' | 'error'
  title: string
  description: string
  agentId: string
  timestamp: string
  metadata: Record<string, any>
}

/**
 * Response shape for the marketing history REST endpoint.
 */
export interface MarketingHistoryResponse {
  events: MarketingActionEvent[]
  total: number
  hasMore: boolean
}
```

### 3.2 WebSocket Payload Types

```typescript
/**
 * Payload emitted via WebSocket when an agent's status changes.
 */
export interface AgentStatusUpdatePayload {
  agentId: string
  agentName: string
  status: AgentStatus
  currentTask: string | null
  taskStartedAt: string | null
  lastActivity: string
  tenantId: number
  companyId: number
}

/**
 * Payload emitted via WebSocket when an agent's task changes.
 */
export interface AgentTaskUpdatePayload {
  agentId: string
  taskId: string
  taskName: string
  taskDescription: string
  status: 'started' | 'in_progress' | 'completed' | 'failed'
  progress: number
  output: string | null
  timestamp: string
}

/**
 * Batch payload for initial full snapshot or periodic refresh.
 */
export interface MarketingAgentBatchPayload {
  agents: MarketingAgent[]
  connections: AgentConnection[]
  timestamp: string
}
```

---

## 4. Java DTO Records

### 4.1 MarketingHistoryController DTOs

```java
// File: backend_new/src/main/java/com/app/controllers/MarketingHistoryController.java

@RestController
@RequestMapping("/api/v1/marketing/agents")
public class MarketingHistoryController {

    /** Live agent DTO returned to the frontend */
    public record MarketingAgentDto(
        String id,
        String name,
        String status,
        String currentTask,
        String lastActivity,
        String avatarUrl
    ) {}

    /** Directed connection between two agents */
    public record AgentConnectionDto(
        String from,
        String to,
        String label
    ) {}

    /** Marketing action event for the history timeline */
    public record MarketingActionEventDto(
        String id,
        String agentId,
        String agentName,
        String actionType,
        String description,
        String timestamp,
        Map<String, Object> metadata
    ) {}

    /** Response for live-status endpoint */
    public record LiveStatusResponse(
        List<MarketingAgentDto> agents,
        List<AgentConnectionDto> connections
    ) {}

    /** Response for history endpoint */
    public record MarketingHistoryResponse(
        List<MarketingAgentDto> agents,
        List<AgentConnectionDto> connections,
        List<MarketingActionEventDto> recentEvents,
        String generatedAt
    ) {}
}
```

---

## 5. WebSocket Events

### 5.1 Client → Server Events

| Event Name | Payload | Description |
|---|---|---|
| `subscribe-marketing` | `{ tenantId: number, companyId?: number }` | Join marketing room for tenant/company |
| `unsubscribe-marketing` | `{ tenantId: number, companyId?: number }` | Leave marketing room |

### 5.2 Server → Client Events

| Event Name | Payload | Description |
|---|---|---|
| `marketing-agent-batch-update` | `MarketingAgentBatchPayload` | Full agent + connections snapshot |
| `marketing-agent-status-update` | `AgentStatusUpdatePayload` | Single agent status change |
| `marketing-agent-task-update` | `AgentTaskUpdatePayload` | Single agent task change |
| `marketing-action-event` | `MarketingActionEvent` | New timeline event |
| `subscribed-marketing` | `{ room, tenantId, companyId }` | Subscription confirmation |
| `unsubscribed-marketing` | `{ room, tenantId, companyId }` | Unsubscription confirmation |

### 5.3 Room Naming Convention

```
With companyId:    marketing_tenant_{tenantId}_company_{companyId}
Without companyId: marketing_tenant_{tenantId}
```

---

## 6. Error Handling

### 6.1 Service Error Responses

All service methods return empty defaults on error rather than throwing:

| Method | Abort Error | Non-Abort Error |
|---|---|---|
| `getLiveAgents()` | `{ agents: [], connections: [] }` | `{ agents: [], connections: [] }` |
| `getActionHistory()` | `{ events: [], total: 0, hasMore: false }` | `{ events: [], total: 0, hasMore: false }` |
| `getAgentConnections()` | `[]` | `[]` |
| `getAgentTasks()` | `[]` | `[]` |

### 6.2 AbortError Detection

```typescript
function isAbortError(error: unknown): boolean {
  if (error && typeof error === 'object') {
    const name = (error as { name?: string }).name
    if (name === 'AbortError' || name === 'CanceledError') return true
    const code = (error as { code?: string }).code
    if (code === 'ERR_CANCELED') return true
  }
  return false
}
```

---

## 7. Authentication

All REST endpoints require JWT authentication via the `Authorization` header:

```
Authorization: Bearer <jwt_token>
```

The JWT token is automatically attached by the `axiosInstance` interceptor:

```typescript
// File: frontend_new/src/utils/axiosInstance.ts

axiosInstance.interceptors.request.use((config) => {
  const token = getAuthToken() // From session/context
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  config.headers['X-Tenant-ID'] = getTenantId()
  return config
})
```

---

## 8. Pagination

The `getActionHistory` method supports pagination via `limit` and `page` parameters:

```typescript
// Example: Fetch page 2 with 20 items per page
const response = await marketingHistoryService.getActionHistory(
  tenantId,
  20,    // limit
  2,     // page
  companyId,
  signal
)

// Response includes pagination metadata
console.log(response.total)   // Total events available
console.log(response.hasMore) // Whether more pages exist
```

---

*🤖 Technical Writer & Diagram Specialist — CLOUD-212 API Contracts Complete*
